# 页面设计合同

`site-design`、`site-builder` 与 `site-check` 共用的设计接口。方向、实现和验收共享这一份事实，不另写互相漂移的规格。

复制到项目的 `.site/design/surface-brief.md` 后填写。这是**填空骨架**：只写本项目的事实与判断，不要照抄本文件的示例文字，也不要在字段里保留说明句。

填写深度随任务变化：

- **quick：** 不创建合同；只检查和修改受影响项。
- **guided：** 填写本轮问题、项目事实、设计推导、选定方向，以及适用的页面、响应式、状态、素材和验收标准。
- **strict 或复杂产品：** 补全所有适用部分及风险证据。

不适用字段直接删除，不为完整感凑内容。体验稿前形成方向部分；方向确认后在同一文件补全实现规格。

**方法不在这里。** 需要"怎么写"时按需读对应 reference，不要凭本节字段名猜标准：

| 本节 | 方法论位置 |
| --- | --- |
| 项目事实、任务交互合同、范围防火墙 | [设计上下文](design-context.md) |
| 设计推导、视觉方向（母题 / 视觉世界 / 构图命题） | [视觉方向](visual-direction.md) |
| 结构方向、视觉风格候选、微调与资产继承 | [体验稿](prototype.md) |
| 排版、色彩、素材、构图、状态、图表核对 | [工艺审查](craft-review.md) |
| Design Token 选择顺序与配方 | [设计 token](design-tokens.md) |
| 落地页与营销转化 | [落地页指南](landing-page.md) |
| 检索候选与实现注意项 | [内置工具链](design-toolchain.md) |

## 设计合同索引

机器可读的合同锚点，供 `site-design` 检查和 `site-builder` 交接校验。只索引正文已定义的编号，不复制正文规格；正文改了编号或内容，索引和旧报告同步失效。填写深度随任务：简单项目只索引适用字段，空列表表示该类对象不适用，不是缺失。

```site-contract
{
  "work_type": "",
  "structure_mode": "",
  "scope_refs": [],
  "required_constraints": [],
  "pages": [],
  "sections": [],
  "responsive": [],
  "components": [],
  "assets": [],
  "copy": [],
  "acceptance": [],
  "unresolved_confirm": [],
  "blocking_missing_assets": [],
  "icon_system": "",
  "icon_exceptions": [],
  "intentional_exceptions": []
}
```

- `work_type`：见 [本轮问题](#本轮问题) 的工作类型。
- `structure_mode`：`single` 无真实结构分歧；`choice` 存在需用户先选的信息拓扑分歧。
- `scope_refs`：首版包含项 `BR-*` 编号，来自 [首版承诺](#首版承诺--原型覆盖)。
- `required_constraints`：等级为 `required` 的 `IC-*`，来自 [任务交互合同](#任务交互合同)。
- `pages / sections / responsive / components / assets`：`PG-* / SC-* / RP-* / CP-* / AS-*`，来自对应正文表。
- `copy`：`TX-*`，来自 [文案台账](#文案台账)。
- `acceptance`：`VA-*`，来自 [视觉验收标准](#视觉验收标准)。
- `unresolved_confirm`：尚未关闭的 `confirm` 项编号；为空才能进入实现。
- `blocking_missing_assets`：缺失且会改变构图或可信度的 `AS-*`；为空才能进入实现。
- `icon_system`：项目采用的图标体系，留空按默认 `lucide`。用户或现有工程明确指定其他体系时如实填写该体系。
- `icon_exceptions`：充当图标的 Emoji/Unicode 例外，每项 `{"char": "✓", "basis": "产品依据"}`；无例外留空。例外须核对识别、一致性和可访问名称。
- `intentional_exceptions`：合同显式豁免的项，检查时跳过；`lint-ui` 把其中路径/通配条目当作免扫文件，其余条目按既有用途豁免对应编号。

## 本轮问题

- **问题：**
- **工作类型：**
- **成果：**
- **方向数量：**

`工作类型` 取 `new-surface | redesign | existing-system | local-change | flow-state`。

## 项目事实

以下四项是派生判断的依据来源，必须填写；空着会在 `prebuild` 阶段阻断。

- **使用者：**
- **主任务：**
- **业务对象：**
- **真实内容：**
- **已确认状态：**
- **后果与约束：**
- **设备与视口：**
- **继承依据：**

## 上下文账本

| 来源 | 精确观察 | 对本稿的作用 | 证据 |
| --- | --- | --- | --- |
|  |  | `inherit | adapt | avoid | unknown` | `measured | derived | inferred` |

## 任务交互合同

- **任务类型：**
- **核心对象及生命周期：**
- **频率、数量与时长：**
- **设备与场景：**
- **风险与可逆性：**
- **导航与工作区原则：**

| ID | 约束 | 等级 | 依据 | 对结构/状态的影响 |
| --- | --- | --- | --- | --- |
| `IC-01` |  | `required | recommended | confirm | excluded` | `measured | derived | inferred` |  |

| Flow | 起点或触发 | 界面响应 | 保留或改变什么 | 等级/待确认项 |
| --- | --- | --- | --- | --- |
| 主路径 |  |  |  |  |
| 成功 |  |  |  |  |
| 失败恢复 |  |  |  |  |
| 中途退出 |  |  |  |  |
| 再次打开 |  |  |  |  |

- **中国本地化条件：**
- **明确排除项：**

## 首版承诺 × 原型覆盖

来源引用可用既有 `BR-*` 编号或精确的条目引用。`covered` 只表示本稿实际演示过该条件，不代表正式能力完成；`simulated` 要写明替代的规则、数据或服务；`missing` 要写清成品必须补什么。

| 来源引用 | 可观察结果 / 验收条件 | 原型位置与覆盖判定 | 正式实现必须补齐 | 交接目标 ID |
| --- | --- | --- | --- | --- |
| `BR-01` 或精确条目引用 |  | `covered | simulated | missing`；页面/控件/状态及证据或限制 |  | `IC-* / CP-* / VA-*` 等适用 ID |

## 设计推导

- **用户路径：**
- **首屏命题：**
- **真实素材主角：**
- **类别默认：**
- **反默认原因：**
- **替代结构：**
- **明确不做：**

`反默认原因` 必须引用至少一条**本索引已声明**的编号（`BR-* / IC-* / PG-* / SC-* / RP-* / CP-* / AS-* / TX-*`）。只写在字段里的编号不算声明——必须同时进入上面的 `site-contract` 索引并在正文有定义，否则自造编号就能自证。`VA-*` 是验收输出，不能作为依据。反默认的理由要能指向具体项目事实；删掉所引用的编号后理由就不再成立，才算成立。

## 结构方向（骨架体验稿）

| 候选 | 主要变化轴 | 首屏结构 | 优点 | 风险 | 结果 |
| --- | --- | --- | --- | --- | --- |
| `方案 A` |  |  |  |  | `pending` |
| `方案 B` |  |  |  |  | `pending` |

## 视觉方向

`母题`、`构图命题`、`细节签名` 同样必须各引用至少一条索引已声明的编号，规则见 [设计推导](#设计推导)。

- **内容事实：**
- **视觉世界：**
- **母题：**
- **色彩来源：**
- **字体角色：**
- **构图命题：**
- **细节签名：**
- **常规 UI 图标体系：**
- **Emoji/Unicode 例外：**
- **素材与许可：**

`视觉世界` 的证据等级取 `measured | derived | inferred`。

## 视觉风格候选（轻量体验稿）

在已定骨架上提供 2 种母题与视觉世界差异显著的方案。此环节坚守轻量：不写复杂业务逻辑，不执行单元测试、合同校验或全套浏览器断言。

| 候选 | 匹配依据 | 母题 | 视觉世界 | 构图命题 | 突出 / 牺牲 | 实现 / 无障碍风险 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `风格一` |  |  |  |  |  |  | `pending` |
| `风格二` |  |  |  |  |  |  | `pending` |

- **交换检查结论：**
- **结构差异证据：**
- **推荐项及理由：**
- **微调与资产继承清单（Demo 即胚胎）：**
  - **继承的 Token 资产：**
  - **继承的 HTML 模板：**
  - **沉淀的验收标准：**

`结构差异证据` 在声明 2 个及以上候选时必填，写主布局容器、核心组件形态、信息密度与首屏视觉重心；缺失或候选间母题与构图命题相同会被 `lint-ui` 阻断。

## 正式实现规格

方向确认后补全本节。这里记录选中方向如何落实，不重新发明范围；缺少会改变页面结构、视觉方向或高风险边界的答案时，回到当前方向决定。

### 方向摘要与体验稿

- **选定方向：**
- **适配理由：**
- **代价：**
- **体验稿：**
- **设计方法来源：**

| 来源 | Query / 章节 / 版本 | 命中或采用 | 拒绝或限制 | 进入合同的位置 |
| --- | --- | --- | --- | --- |
| 项目事实 / 成熟设计系统 | 文件、页面或版本 | 继承的机制 | 冲突或例外 | 方向 / Token / 状态 |
| `site-design` 内置方法 | `direction` / `craft-review` | 实际采用原则 | 不适用项 | 方向 / `VA-*` |
| `design-intelligence` | Query、Domain / Stack、`2.13.0` | Result ID 或 `no_verified_match` | 高排名未采用项及理由 | 方向 / Token / 状态 |

只记录真正使用过的来源。检索没有决定权：返回的阈值、库选择、色值与 import 代码都不是项目标准；一个决定只有一个权威来源，冲突时按 [内置工具链](design-toolchain.md) 的授权分层表裁决并记入“拒绝或限制”。

### 页面地图

| ID | 页面 / 路由 | 用途 | 主任务或转化 | 优先级 | 动态 / CMS |
| --- | --- | --- | --- | --- | --- |
| `PG-01` |  |  |  | `primary | secondary | utility` |  |

只覆盖已确认首版。明确排除的未来页面留在 `brief.md`，不以禁用入口、空导航或"敬请期待"混入本轮产品。

### 区块清单

| ID | 页面 | 顺序 | 区块职责 | 内容类型与来源 | 布局模式 | 状态 / 动态数据 |
| --- | --- | --- | --- | --- | --- | --- |
| `SC-01` | `PG-01` | 1 |  |  |  |  |

每个区块只有一个主要职责和一个主要行动或结论。页面结构来自主任务，不机械套用同一组 Hero、卡片与 CTA。

### Design Token

已有设计系统时记录语义映射和例外，不复制第二套变量。

- **颜色：**
- **排版：**
- **空间与布局：**
- **形状与层次：**
- **动效：**

颜色要覆盖 `canvas / surface / text / text-muted / accent / on-accent / border / border-control / focus / success / warning / error` 的实际值、用途及关键相邻色对；排版按 display / 标题 / 正文 / UI / 数据 / 代码分配字族、字号、行高、字重和字距，含中文回退与数字对齐。

### 响应式合同

断点从内容开始失效的位置得出，不按设备名称凑数；至少覆盖 375px、768px、1440px。

| ID | 范围 / 代表视口 | 栅格与 gutter | 导航 | 内容重组 | 主行动 | 素材裁剪 |
| --- | --- | --- | --- | --- | --- | --- |
| `RP-01` | 窄屏 / 375px |  |  |  |  |  |
| `RP-02` | 中屏 / 768px |  |  |  |  |  |
| `RP-03` | 宽屏 / 1440px |  |  |  |  |  |

### 组件与状态

只列正式页面实际出现的交互类型，不为不存在的网络、权限或数据状态造界面。

| ID | 组件 / 模式 | 出现位置 | 状态 | 可见反馈 | 键盘 / 触控 | 内容极限 |
| --- | --- | --- | --- | --- | --- | --- |
| `CP-01` |  |  |  |  |  |  |

| 来源 / 组件 ID | 触发条件 | 用户看到什么 | 保留/改变什么与恢复操作 | 原型覆盖 / 正式补齐 | 验收 ID |
| --- | --- | --- | --- | --- | --- |
| `BR-* / IC-* / CP-*` |  |  |  |  | `VA-*` 或业务检查引用 |

每态说明发生了什么和用户能做什么。这张表补齐具体状态设计，不复制另一份首版范围。

### 文案台账

| ID | 页面 / 区块 | 文案槽位 | 来源 | 长度目标 | 审核或缺口 |
| --- | --- | --- | --- | --- | --- |
| `TX-01` |  |  | `user | existing | reference | ai-draft | placeholder` |  |  |

AI 起草和占位文字必须让创作者知道；事实性承诺、价格、履历、评价和数据没有来源时不得写成正式文案。标题、按钮、导航等长度敏感位置写字符目标和最长样例。

### 素材地图

| ID | 页面 / 槽位 | 预期尺寸 / 比例 | 来源与性质 | 许可 / 日期 | 裁剪与 alt | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `AS-01` |  |  |  |  |  | `ready | missing | replace` |

真实素材、生成素材和示例素材分别标注。缺失素材若会改变构图或可信度则阻断实现；不阻断时使用有设计的缺失状态，不以灰框或无来源图片冒充完成。

### 视觉验收标准

把选定方向写成可观察、可在真实页面上取证的断言，每条绑定页面、状态与视口。

| ID | 页面 / 状态 / 视口 | 可观察标准 | 检查轴 | 阻断 |
| --- | --- | --- | --- | --- |
| `VA-01` |  |  | `core_task | visual_desktop | visual_mobile | reopen` | `yes | no` |

`可观察标准` 里出现"高级 / 现代 / 像参考 / 时尚 / 优雅 / 大气 / 精致"这类无法判定的用词时，`prebuild` 阶段会给警告，`precheck` 阶段直接阻断：`VA-*` 是 `site-check` 后续浏览器验证的唯一依据，写不出可判定断言会让下游全部验证落空。

最低覆盖：选定方向与首屏重心、色值和实际相邻色对、排版角色与中文回退、空间与栅格、主任务完整路径、组件状态、窄屏信息保真、素材清晰度与许可、键盘焦点和触控尺寸、减少动态效果。参考还原项目另写明参考页面、状态、视口与允许偏差；原创方向不要求像素级复刻。

### 纵向切片

正式实现按少量可独立体验的纵向切片排序，不按数据层、接口层和页面层横向铺开。每条贯穿完成该能力所需的数据、规则、界面和检查，引用同一份合同 ID。方向确认后随实现补齐；简单项目可只列一条。

首列维护 Checklist 状态（`[ ]` 待执行 / `[-]` 执行中 / `[x]` 已闭环），作为 Agent 外部状态锚点防遗忘；最后一列记录最短验证事实证据，作为打勾门禁。

| 状态 | 切片 | 引用 ID | 成功路径 | 最可能失败路径 | 必要状态 | 验证证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `[ ]` |  | `BR-* / IC-* / PG-* / SC-* / CP-* / VA-*` |  |  |  |  |

切片推进时严格执行三步法（读表锁定 ➔ 代码微计划与最小增量 ➔ 事实检查通过后打勾），把返工前移到切片内。引用的 ID 在正文缺失先回 `site-design` 补齐，不杜撰。所有切片打勾后做一次集成构建，再由 `site-check` 只读浏览器验证。

## 体验稿完成条件

- **页面：**
- **状态：**
- **视口：**
- **设计判断：**
- **任务/状态判断：**
- **机械判断：**
- **工艺核对：**
- **证据位置：**

`工艺核对` 写排版（角色分配、字阶、行高、标点）、色彩来源与素材许可、构图重心与细节签名各自的结论。

## 方向确认与交接

- **完整方向：**
- **用户原话或既有委托：**
- **选择理由：**
- **方向来源：**
- **模拟范围：**
- **覆盖交接：**
- **未关闭发现：**
- **仍需确认：**
- **交给 `site-builder` 的实现事项：**
- **交给 `site-check` 的验收事项：**

`完整方向` 取 `pending | confirmed`，`方向来源` 取 `user_selected | reference | existing | delegated`。`未关闭发现` 写来源、预期、实际、位置、影响和复验状态，不把"已改"写成"已验证"。

### 使用边界

方向部分必须在第一次视觉实现前形成；正式实现规格在完整方向确认后、正式开发前补齐。候选满足 `required`、不暗示 `excluded`，对 `recommended` 的偏离写明依据；会改变核心范围或风险的 `confirm` 交回 `site-brief`。无法写出项目特定的反默认、母题来源和替代结构时，先补事实，不用配方、色板或风格标签填空。用户只确认一次完整方向；结构发生实质变化时，先更新视觉方向，再重新确认整个方向。内部规格补全不新增用户门禁，但适用字段缺失时不得把猜测转移给实现者。
