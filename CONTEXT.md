# 领域术语

## 产品与用户

**创作者**：借助 Coding Agent 建网站的人，不一定是网站建成后的最终使用者。
**最终用户**：网站完成后实际使用它的人；同一项目可以有多类最终用户。
**完整愿景**：创作者最终想达到的整体目标，不等于本轮承诺。
**首个可验证版本**：本轮确认、开发和验证的中小型成果，至少包含一条可完整体验的核心任务。
**纵向切片**：从必要数据和规则贯穿到可见结果、能够独立体验和验证的一项能力。

本项目的定位是：让非技术中文用户只描述现实目标，四个 Skill 在内部完成收敛、设计、实施和验证。流程复杂度不能转嫁给用户，也不能为了省 token 牺牲任务正确性和生成质量。

## 理解与确认

**当前前沿**：前置事实已清楚、当前可独立回答并会改变首版的一组问题。
**方案确认**：创作者确认首版用户、场景、核心任务和范围，或在低风险范围明确委托 Agent 决定。
**结构确认**：创作者在多个页面组织方案中选定一个；它不等于视觉确认。
**视觉确认**：创作者确认同一结构下的视觉表达；它不自动构成开发授权。
**开发授权**：创作者允许正式修改工程。可来自一开始持续有效的建设委托，也可来自看完方案后的明确继续；视觉称赞本身不算。
**全权委托**：允许 Agent 在已确认、低风险范围采用推荐项连续推进，不覆盖费用、账号、敏感数据、公开部署或显著范围变化。
**确认记录**：门禁工具保存的用户原话、时间、标注和摘要。它记录 Agent 的声明，不是用户同意的证明。
**标注等级**：`agent-reported | quote-matched`；只表示原话留存强度，不表示同意已被验证。

**视觉体验稿**：用真实或接近真实内容回答结构、层级和视觉表达问题的低成本页面。
**流程体验稿**：用示例数据和少量交互回答操作顺序、状态和规则问题的低成本演示。
**协作回执**：Skill 给另一个 Skill 的紧凑结果；不是要求创作者阅读的新文档。
**通俗交付**：用非技术语言说明入口、使用顺序、已完成范围、数据位置和限制。

## 设计三层

视觉质量需要三个互相独立的决定，不能再用一个 `visual_source` 混装：

```yaml
direction:
  source: existing | reference | project-derived
  evidence: []
  motif:
  composition:
  project_signature:

grammar:
  source: existing-system | packaged-spec | project-tokens
  spec_id:
  deviations: []

patterns:
  selected: []
  adaptations: []
  rejected: []
```

**视觉方向（direction）**：这个项目为什么这样表达。来源只能是已有项目、用户指定参考或项目事实推导。
**视觉世界**：业务对象所属的材料、媒介与环境，是推导方向的采样源，不是风格名。
**母题（motif）**：从视觉世界收敛出的一个项目特定形式决定；换成另一个项目仍成立的只是通用装饰。
**构图命题（composition）**：主次关系及其靠尺度、位置、对比、留白或运动成立的可证伪陈述。
**项目签名（project_signature）**：一处服务真实内容、可重复使用且不能无损换到别的项目的细节。

**样式语法（grammar）**：颜色、字体角色、边界、组件状态和布局尺度如何保持一致。随包规范只可能成为 grammar，不能替项目产生 direction，也不能创造需求。
**实现模式（patterns）**：为已确认任务复用的代码与交互模式。模板属于这一层，只能在任务合同和 direction 之后选择，并记录采用、适配和拒绝。
**偏离（deviation）**：实现与选定 grammar 不一致的 `{ rule, reason, evidence }`；有依据的偏离允许，静默偏离失败。
**DesignPacket**：给 builder/checker 的紧凑设计交接，只含事实、任务合同、三层决定、验收条件和最多三个候选。

旧项目的 `visual_source` 仍可读取：`existing/reference/original/spec` 在生成 DesignPacket 时映射到新三层并标记 `legacy_visual_source`，不得为了升级重写旧项目；新记录只写新模型。

## 选择与质量

**选择画像**：规范或模板检索所需的 `task / content_shape / content_subject / audience / trust_posture / asset_conditions / interaction_intensity / primary_device`。行业名称不参与选择；只有行业时信息不足。
**硬约束**：明暗、素材是否可得、减弱动效和可接受视觉风险等不可用评分抵消的条件，必须先过滤。
**弃权**：没有有意义的语义命中时返回 `no_match`，不用“最接近”冒充匹配。
**真实渲染验证**：浏览器读取 computed style、实际视口和操作结果；静态源码扫描不属于这一类。
**用户运行时闭环**：生成后自动执行的渲染、核心任务、状态和再次打开检查与修复。
**维护者评测闭环**：Skill 或资产发布前运行的固定夹具、双真人评分、token 与耗时比较；普通用户不会操作它。
**not_run**：缺少条件或证据时的诚实状态；不是推定通过。

## 协作状态

**事实、决定与假设**：事实可调查；决定需要创作者取舍；假设是在低风险未知上采用并明确记录的临时答案。
**任务交互合同**：按 `required / recommended / confirm / excluded` 约束主路径、成功、失败恢复、中途退出、再次打开和本地化，不按行业套页面。
**项目租约**：同一项目同一时间只允许一个 Writer 或 Checker 的写入凭证。
**交接冻结**：Writer 停止自有服务后对正式源码和设计交接计算的指纹；源码变化使当轮验收失效。
**验收凭据**：`site-check` 生成并绑定源码指纹的内容寻址矩阵。它能发现误改，不证明证据语义正确。
**不可逆动作**：付费、系统级安装、第三方授权、敏感数据和公开部署等无法靠一轮迭代挽回的操作；必须单独获得授权。
