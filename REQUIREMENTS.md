# 渐进式建站 Skills：需求基线

> 状态：已确认  
> 适用范围：一起安装并协作运行的 `site-builder`、`site-brief`、`site-design`、`site-check`
> 目标用户：以中国大陆使用环境为主、没有编程背景的中文用户

## 1. 产品定位

这是一套供 Coding Agent 使用的建站 Skills。用户不需要理解开发阶段、技术栈或 Skill 名称，只需用自然语言描述想法、参考资料、现有项目或修改目标。

系统的核心价值不是更快地产生更多代码，而是：

1. 通过启发、反问、比较和回放，帮助用户说清第一版真正需要解决的问题；
2. 在正式开发前，用低成本、可见的成果分两步确认：先页面结构，再视觉风格；
3. 由 Agent 处理依赖安装、运行、构建、验证和常见环境问题；
4. 将大型愿景收敛为可体验、可验证的中小型首版，减少错误方向造成的时间和 Token 浪费。

## 2. 核心术语

项目统一术语及其严格含义见 [`CONTEXT.md`](CONTEXT.md)。本需求使用其中的“创作者”“最终用户”“完整愿景”“首个可验证版本”“当前前沿”“视觉体验稿”“流程体验稿”“纵向切片”“协作回执”“方案确认”“视觉确认”“开发授权”“全权委托”“高风险事项”“事实、决定与假设”和“通俗交付”，不在各 Skill 中另行发明近义阶段名。

## 3. 能力结构与协作

四个 Skill 作为一套安装。用户不需要知道它们的名称或手动切换；Agent 根据自然语言目标选择入口，Skill 之间通过明确调用与项目记录协作。每项规则只有一个 Skill 负责维护，调用者依赖其成果，不复制其内部流程。

### 3.1 `site-builder`：默认入口、编排与实施

负责识别请求、调用其他 Skill、管理确认记录与开发门禁、生成实施计划、按可体验切片正式开发、根据检查结果修复并通俗交付：

```text
识别现状
→ 需要时调用 site-brief
→ 通过方案门禁
→ 需要时调用 site-design
→ 通过开发门禁
→ 生成实施计划并正式开发
→ 调用 site-check
→ 修复并复验
→ 通俗交付和迭代
```

局部修改和 Bug 按影响跳过无关阶段。只有 `site-builder` 可以判断开发授权和正式交付；协作状态统一交给 `site-brief` 原子记录。

### 3.2 `site-brief`：共享理解与首版收敛

负责调查可查事实、澄清高影响决定、识别业务词冲突、区分完整愿景与首个可验证版本，并按需维护 `.site/brief.md`。它可以独立交付需求梳理成果，也可以被 `site-builder`、`site-design` 调用；不得制作视觉稿、进入正式开发或授予开发权限。

### 3.3 `site-design`：共享视觉方案与设计能力

负责分析参考、制作结构方向与风格方案或流程/状态体验稿，以及为 `site-check` 提供只读视觉审查。它是整套 Skills 中**产出视觉质量**的一环：先从活跃代码、现有产品、品牌和真实素材建立上下文，再通过内置工具链扩大候选并按项目事实筛选。工具链融合项目化的方向方法、工艺规则与 UI/UX Pro Max `2.13.0` 的 MIT 检索器和数据；统一入口为 `site-design/scripts/design.py catalog | research | validate`，不要求用户额外安装。方向推导由 `visual-direction.md` 承担，排版、色彩素材、构图、任务界面和评审证据统一由 `craft-review.md` 承担，避免 Agent 在重复文档间来回查找。

页面**结构**的选择与**风格**的选择是两个决定：用户选定结构只完成第一步，必须再在同一结构上给出完整风格方案。默认给两个母题层不同的候选（用交换检查判定），第三个只有在能回答新的重要取舍时增加。每个候选说明上下文依据、适配点、突出内容和取舍，Agent 给出推荐但等待用户选择；风格名称只用于沟通，不能脱离项目证据产生方向。背景不足时调用 `site-brief`；用户要求把设计应用到正式工程时由 `site-builder` 实施。不得把结构选择或视觉选择解释为开发授权，也不得把配方、色板或随机风格标签当作设计方向提交。

行业画像只用于召回可能影响设计的信任、隐私、时效、可逆性、监管与专业性后果，并按 `within_scope / alternative_only / requires_confirmation / discard` 分类。行业名称本身不是项目证据；外部画像的固定风格、结构、色板、字体、CTA 和 `must_have` 不得覆盖用户材料、成熟设计系统、真实内容或已确认范围，也不得创造首版能力。

结构候选前先形成一份轻量任务交互合同，并保存在同一份 `.site/design/surface-brief.md` 中，不新增 Skill 或项目产物。合同按核心对象生命周期、使用频率与数量、设备分工、风险与可逆性约束导航/工作区、主路径、成功、失败恢复、中途退出、再次打开、中国本地化条件和排除项；不按行业套页面。每条约束分为 `required / recommended / confirm / excluded`：必需项贯穿设计、实施与检查，建议项允许被更具体的项目事实覆盖，待确认项不得伪装成已决定能力，排除项不得出现在页面、导航、文案或模拟数据中。行业资料与竞品只能提出建议或待确认项，不能单独创造必需能力。

### 3.4 `site-check`：共享独立验证能力

负责静态/构建、核心业务、视觉和再次打开验证。视觉轴调用 `site-design` 的只读审查分支。被 `site-builder` 调用时返回发现，由 `site-builder` 修复后再次检查；独立调用默认只报告，不修改源码，也不自行宣布项目已交付。

## 4. 入口判断

`site-builder` 负责默认路由；用户明确只要梳理、设计或验收时可直接进入相应 Skill。Agent 应识别用户处于哪一种状态：

- 完全没有想法：提供 2～3 个不同的创意方向；
- 有想法但不完整：澄清会改变体验或实现边界的未知；
- 有明确参考：先查看参考并区分还原、借鉴或功能相似；
- 已有设计：理解并沿用，除非用户要求改变方向；
- 已有工程：读取实际代码和约定，判断继续、修改或修复；
- 局部修改或 Bug：直接处理，不机械重走完整流程；
- 只梳理需求：进入 `site-brief` 范围；
- 只做设计：进入 `site-design` 范围；
- 只做验收：进入 `site-check` 范围。

## 5. 创意探索与需求澄清

### 5.1 没有明确想法

结合用户角色、实际场景和痛点，提供 2～3 个实质不同的方向。每个方向说明：

- 解决什么问题；
- 谁会使用；
- 最核心的操作；
- 首版大致包含什么；
- 相对复杂度；
- 推荐项及理由。

### 5.2 已有想法但不完整

根据用户表达能力动态提问，默认每轮 3～5 个相关问题；未知很少时可以少问，不为凑数提问。

提问必须：

- 使用普通人的语言和现实例子；
- 优先提供 2～4 个具体选项、推荐项和理由；
- 允许用户回答“按推荐方案”；
- 已知信息不重问；
- 不把框架、数据库、部署方式等技术选择推给非技术用户；
- 将未知分为事实、决定和假设：项目、参考与环境能回答的事实由 Agent 调查，只有产品取舍才询问创作者；
- 每轮只问前置条件已清楚、彼此不依赖的当前前沿问题，用户回答后再计算下一轮。

澄清在首个可验证版本不再存在会改变核心体验、范围或高风险边界的未知时结束，不要求穷尽完整愿景的所有未来分支。复杂或跨会话项目中，已确认结论及时写入 `brief.md`，不在最后凭记忆批量总结。

优先澄清：核心用户与场景、最重要任务、核心对象如何开始/完成/取消、使用频率与数量、角色与设备分工、操作后果和可逆性、数据是否跨设备或多人共享、外部服务、本地化条件与首版范围。技术问题应翻译为结果差异，例如询问“换电脑后是否仍要看到记录”，而不是询问存储技术。

## 6. 确认记录与开发门禁

本节说明产品意图，不重复命令级校验。确认模型的统一术语见 [`CONTEXT.md`](CONTEXT.md)，状态字段、原话记录、租约和拒绝条件以 [`site-brief/references/state.md`](site-brief/references/state.md) 为准。

### 6.1 方案确认

制作视觉体验稿前，原则上应明确：

- 完整愿景；
- 首个可验证版本；
- 核心用户和场景；
- 一条核心任务；
- 首版包含与暂不包含的内容；
- 尚未确认的 Agent 假设。

简单、低风险项目中，用户明确说“你决定”或“按推荐继续”后，Agent 可以记录推荐方案并继续。涉及多人权限、云端数据、隐私、费用或明显大型范围时，必须等待关键方案确认。

### 6.2 开发授权

体验稿完成后，Agent 应收集反馈、完成必要调整，并用通俗语言回放选定结构、选定风格与首版范围。为减少非技术用户的认知负担，有充分依据时默认只给一个推荐结构；只有结构会实质改变主任务或重要取舍时才比较多个。风格默认给两个母题层不同的完整候选，第三个只有在能回答新的重要取舍时增加。用户选定页面结构后必须先给出风格方案：只有结构确认时状态是“结构已确认、风格待选”，还不能进入开发。

视觉确认后，`site-design` 应在 `.site/design/surface-brief.md` 的同一页面设计合同中补全页面地图、区块清单、Design Token、响应式合同、组件状态、文案台账、素材地图和 `VA-*` 可观察视觉验收标准。补全普通实现细节不新增用户门禁；发现会改变结构、视觉方向或高风险边界的事项时，回到对应决定。`site-builder` 与 `site-check` 必须消费这份合同，不各自重建视觉规格。

开发授权按**完整请求和相邻问答表达的行动意图**判断，不按固定口令。用户一开始就明确要求建设、实现、修改、修复或完成正式成果时，这是一项持续实施委托；它不允许跳过方案、结构或视觉确认，但在低风险且核心范围未实质变化时，前置决定全部确认后即可用该条独立原话记录授权，不再重复索要“开始开发”。用户只要求梳理、设计、比较或查看体验稿时，不存在这项委托；此时 Agent 要先明确说明“确认当前版本后将进入正式开发”的后果，用户紧接着表达认可、无异议或继续即可授权，即使省略动作词。相同字面若是在回答审美、结构或是否继续比较，就只完成那个决定。

每条答复按发生时正在回答的决定归属，不得把较晚的笼统肯定倒填成较早的方案、结构或视觉确认，也不得用同一条原话同时完成两类确认记录。语境仍无法确定行动后果时，只补问这一项；对用户不展示门禁表、脚本报错或规定口令。出现高风险事项或显著范围变化仍须暂停。

### 6.3 门禁记录的是声明，不是同意的证明

确认门禁不声称能验证用户同意，因为用户的判断不在任何可读文件里，而门禁要约束的 Agent 恰好能写所有文件。因此要求：

- 记录必须逐字抄录用户原话（`--quote`），不得改写成 Agent 的措辞；调用 Skill 先依据当前阶段、上一问及其行动后果判断这句话属于哪项决定，工具拒绝空原话，并按引语内容阻止同一句连过两道门禁；
- 会话记录只作为**可选增强**：能定位就升级标注为 `quote-matched`，读不到或读不懂宿主格式只降级为 `agent-reported`，**任何宿主上都不阻塞流程**；
- 每条记录带 `basis_note`，明说它不是同意的证明；不得把 `delivered` 或标注读成“已核实用户同意”；
- 页面结构与视觉风格各有一条确认记录，同一句原话不能同时充当两者；只记录了结构时不得写 `visual_confirmed=true`；
- 交付时把各道确认的原话回放给用户本人核对（`consent_replay`），用户否认任何一条即须先 `reopen`。**这是全流程中唯一真正验证同意的环节。**
- 装系统软件、付费、第三方授权、真实敏感数据和公开部署不属于本机制：它们是动作授权而非阶段跃迁，按宿主能力分档拦截，全权委托不覆盖。

## 7. 视觉与流程体验稿

视觉体验稿确认视觉风格、信息层级、核心布局与内容表达；流程体验稿确认操作顺序、状态变化或业务规则。两者都是回答明确问题的低成本可见实验，不提前开发半个产品。

在结构候选之前，`site-design` 先从已确认事实形成任务交互合同。合同只约束信息架构、状态和范围，不规定视觉外观：SaaS 或内部运营的高频批量任务可以推导稳定工作区和连续反馈，教育任务可以推导当前任务与反馈连续性，但不能因此默认 KPI 首页、批量操作、账号、长期进度、积分、证书或其他平台能力。面向中国用户时核对中文输入、日期时间、数字金额、地址电话、触屏和弱网等真实条件；微信、短信、二维码、支付、实名或未成年人数据处理仍须由范围与风险确认，不能以本地习惯扩权。

**"接近成品的视觉质量"是可判断的，不是形容词。** 它由四件事决定，缺一项就退回模板：方向有来源（能说出母题来自哪条业务事实）、字面成立（角色分配、字阶、行高、行长与中文排印无硬伤）、材料真实（颜色有采样来源、素材有出处与许可、缺口如实显示）、构图有重心（主任务获得对应视觉重量，缩小到 25% 仍有结构与一处服务内容的细节签名）。推导方法见 `visual-direction.md`；四条判据、排版、色彩素材、构图、任务状态、图表和证据协议统一见 `site-design/references/craft-review.md`。

视觉检索结果、token、配方与色板只做候选发现或方向形成后的实现校准：它们不产生母题、不产生信息架构，**也不能作为方向提交给用户**。查询、命中 ID、采用和拒绝理由写进页面设计合同；没有验证命中时明确记录，不能伪造。写不出母题来源和匹配依据时，方向尚未形成，不得进入风格候选与 token 选择。风格候选先按受众、任务、内容主角、信任姿态、素材条件和交互强度匹配，再翻译成项目自己的视觉表达；候选必须在**母题层**不同，不是配色层不同。判定方法是交换检查——互换两个候选的配色与字体后差异基本消失，它们就是同一个方向的两种上色。

工艺与机械检查必须在**实际渲染**上进行：读渲染后的 computed style 与真实相邻色对，不用源码声明或 token 表里的相邻关系代替；素材来源、许可与裁剪须在展示时一并说明。

默认策略：

- 只做一个简单核心页面；
- 使用真实或明确标注的示例内容；
- 达到接近成品的视觉质量（按上面四条判据核对，而不是靠"看着还行"）；
- 只在关键操作存在明显歧义时增加少量点击或状态；
- 不连接正式后端，不建设完整数据库，不铺满所有页面；
- 告知用户哪些效果是模拟的、哪些素材是示例或缺口，以及本轮需要反馈什么。

方向数量按不确定性决定：

| 情况 | 处理 |
| --- | --- |
| 完全没有视觉偏好 | 有充分依据时先给一个推荐结构；存在重要结构分歧时比较两个。结构选定后默认给两个完整风格方案 |
| 有明确参考 | 1 个针对性方向 |
| 已有成熟规范 | 沿用，不重复选稿 |
| 用户全权委托 | 1 个推荐方向 |
| 只对色彩或字体不确定 | 同一页面展示局部变体 |
| 主要分歧是操作流程 | 1 个方向加少量状态或点击 |

多个方向应使用相同业务内容和相近视口，比较信息组织、主次、密度或表达，不只是换色，也不要求每套完整。

体验稿开始前应写清本轮要回答的问题。外观、布局或内容层级不明确时分两步制作：先**结构方向**（页面怎么组织），用户选定后再在同一结构上做**风格方案**（同一个页面长什么样），两步各记录一句用户原话；操作顺序、状态变化或业务规则难以通过对话判断时制作轻量流程/状态演示，显示当前状态和少量典型场景，默认只使用内存与示例数据。

体验稿可以丢弃重写，也可以在质量合适时演进为正式代码；Agent 必须说明采用哪种方式。多方向可在相同页面上下文中提供轻量切换，确认后删除切换器和落选实现。

## 8. 范围和风险控制

不限制用户提出完整愿景，但本轮正式实施应保持为中小型、可验证范围。大型想法应保留愿景，再推荐一条最有价值的核心流程作为首版，而不是直接承诺完整实现或简单拒绝。

以下情况通常需要暂停确认：

- 登录、多人角色或权限；
- 云端保存、真实敏感数据或未成年人数据；
- 支付、公开发布或持续费用；
- 第三方账号、密钥、域名或用户本人授权；
- 两项需求冲突；
- 两种方案会显著改变体验或工程结构；
- 必须显著缩减已确认范围。

普通技术选型、项目内依赖、构建错误和样式修复由 Agent 自主处理。

## 9. 项目记录

四个 Skill 在正文使用统一的 `reads / writes / schema / handoff / evidence` 契约，并以 [内部回执 schema](site-brief/references/context-contract.md) 引用已有文件、稳定 ID、证据与未决项；不引入平行上下文目录，也不为简单修改创建状态。门禁工具在成功跃迁后派生 `.site/session-state.json` 作为恢复提示；它不参与门禁或冻结指纹。恢复时重新读取真实状态、租约、指纹和检查凭据，过期、缺失或损坏快照不阻塞；`show` 与 Checker 均不补写快照。

记录按需创建，不向简单修改强加协议。默认只保留四个概念：

```text
.site/
├── brief.md                 # 用户和 Agent 均可读
├── design/surface-brief.md  # 设计、建设与验收共享的页面设计合同
├── implementation-plan.md   # 体验确认后、正式开发前创建
└── state.json               # 最小恢复状态
```

`brief.md` 使用通俗语言记录目标、完整愿景、首版范围、核心流程、已确认决定、假设、暂不包含内容、体验稿要回答的问题、选定页面结构、选定视觉风格和当前进度。`site-brief` 是文件的协调写入者：自行维护需求与范围，并记录 `site-design` 返回的有依据设计结果。

`design/surface-brief.md` 由 `site-design` 维护。体验稿前记录项目事实、任务交互合同、结构候选与视觉方向；视觉确认后补全正式实现规格，并用稳定 ID 连接页面、区块、响应式、组件、文案、素材与视觉验收。它只保留设计决定，不复制 `brief.md` 的首版范围或 `implementation-plan.md` 的工程步骤。简单修改只补受影响项；新建、整体改版、主流程或信息结构变化须覆盖所有适用部分。

`implementation-plan.md` 由 `site-builder` 在体验确认和开发授权后创建，主要给 Agent 使用。实施顺序采用少量可独立体验的纵向切片；每条说明用户获得的完整能力、前提、操作、可观察结果、失败恢复、中途退出、适用状态、交互约束、视觉落实、必要依赖和检查。计划逐条映射已确认首版能力、页面设计合同和明确排除项：`required` 必须实现，`recommended` 采用或记录有依据的偏离，`confirm` 先取得决定，`excluded` 明确不实现；`PG-* / SC-* / RP-* / CP-* / TX-* / AS-* / VA-*` 直接引用，不复制合同全文。状态按真实风险选择，不机械穷举或扩张范围。另记录业务规则、数据生命周期、角色权限、外部服务、技术选择和实施顺序。简单修改无需创建。

`state.json` 至少能表达当前阶段、方案/结构/视觉确认情况、开发授权、是否获得全权委托和下一步。状态文件只由 `site-brief` 的门禁脚本 `scripts/state.py` 写入：调用者给出职责内判断与依据，脚本校验门禁后原子替换并递增修订，其他 Skill 不得用编辑工具直接改写。`site-design` 提供结构与视觉判断，`site-builder` 提供开发授权、实施和交付判断，`site-check` 只返回检查结果。上游范围或方向发生实质变化时必须清除不再适用的下游确认。推荐阶段：

- `discovering`
- `concept_review`
- `visual_drafting`
- `visual_review`
- `ready_to_build`
- `building`
- `verifying`
- `delivered`
- `blocked`

门禁脚本同时维护项目级 Writer/Checker 租约、交接记录和验收凭据：同 owner 重复 `claim` 幂等，不同 owner 不得无理由覆盖，`release` 必须匹配 owner；`handoff` 校验 Writer 的 PID 已结束、端口已释放，登记正式服务必须提供存活 PID、监听端口和项目内根目录，并冻结包含 `.site/design/**` 的源码指纹；`start-verify` 在源码变化时拒绝交接；Checker 只凭当前源码上的失败矩阵原子 `reopen` 给 Writer。`delivered` 只能绑定文件名、内部 ID 与内容摘要一致、当前指纹一致、档位最低轴齐全且阻断项全部 `passed` 的 `check_id`，源码变化后旧凭据自动失效。

确认原话必须保留精确空格和换行并单独计算 `quote_sha256`；防重复使用另一个规范化摘要，兼容旧记录。每次状态写入追加不递归的前后摘要、动作、时间和可用的租约 owner/role。状态历史、租约和内容寻址凭据用于减少漏步骤、误覆盖与状态漂移，不构成针对拥有本地写权限的恶意 Agent 的安全边界。

已有旧协议项目可以继续读取原记录；不得因升级而覆盖或清空原文件。

## 10. 开发与环境权限

Agent 可以自主安装项目目录内的依赖、创建虚拟环境、运行开发服务器、处理端口、构建、测试和修复错误。

以下操作需要先用通俗语言说明影响并获得同意：

- 安装 Node.js、Docker、数据库或浏览器等系统级软件；
- 获取管理员权限或修改系统环境变量；
- 使用付费服务或创建云资源；
- 登录第三方平台；
- 获取密钥、域名或真实业务数据；
- 可能影响其他项目或系统的操作。

Skills 遵循通用 Agent Skills 目录规范并作为一套安装。核心文档使用规范 Skill 名称描述协作，不绑定某个宿主的斜杠命令或专用工具语法；宿主适配文件可提供相应入口。宿主无法分派其他 Skill 或独立 Agent 时，在同一 Agent 中执行等价的独立阶段并说明限制；缺少浏览器或执行能力时降级并如实说明未验证范围。

## 11. 已有项目与小修改

所有正式源码修改由 `site-builder` 实施。改文案、局部颜色/间距、修复按钮、逻辑或运行问题时直接处理；重做首页、改变导航或核心流程、整体换风格，或用户明确要求“先给我看看”时，先调用 `site-design` 制作轻量体验稿。

正式开发开始后，只有实质产品取舍或必须由用户完成的外部条件才打断用户；其余工作连续完成。

## 12. 验证与交付

`site-builder` 在正式实现后调用 `site-check`，对本轮范围执行适用的静态/构建、核心业务、视觉和再次打开检查；检查矩阵必须声明档位、非空选择原因与当前真实浏览器能力，每项标记检查轴。`full` 始终表示 `static_build`、`core_task`、`visual_desktop`、`visual_mobile`、`reopen` 五轴：真实浏览器可用时五轴均须有阻断项；不可用时 `static_build` 与 `core_task` 仍须阻断，后三轴分别保留非阻断 `not_run` 与恢复条件。浏览器是条件能力，不在设计或验收阶段要求创作者安装；但只能由浏览器证明的持久化、导入导出或复杂交互仍须在 `core_task` 中保持阻断 `not_run`，不得借能力降级交付。`smoke` / `targeted` 至少包含受影响的阻断 `core_task`。矩阵逐条映射已确认首版能力、交互合同的 `required / excluded`、`recommended` 的采用或偏离依据、`confirm` 的处理结果、页面设计合同中适用的 `PG-* / SC-* / RP-* / CP-* / TX-* / AS-* / VA-*`，以及各纵向切片的成功、失败恢复、中途退出和适用状态，缺失映射不得静默省略。新建、整体改版、主流程或信息结构变化缺少页面设计合同的适用部分时，Checker 将“设计交接不完整”记为阻断失败，不自行补规格。有真实浏览器时，视觉轴由 `site-check` 调用 `site-design` 只读审查，实际渲染代表性的核心页面和复杂操作表面，核对跨页面视觉继承、控件状态、动效节奏与减弱动效行为；原创设计不默认要求像素级复刻。Writer 与 Checker 严格串行：Writer 先停止自有体验稿和开发服务并交接冻结指纹，Checker 全程只读，只把矩阵凭据写到 `.site/checks/`。内容寻址 `check_id` 用于发现 artifact 误改，不替代 Checker 对证据语义的判断。`site-builder` 修复范围内问题后必须引用失败矩阵取回 Writer、重新交接并复验，旧 `check_id` 不得沿用。宿主支持独立 Agent 时优先隔离检查上下文，减少自行实现、自行判定的偏差；用户试用不能替代 Agent 能执行的验证。

不强制生成 `acceptance-guide.md`。交付消息用通俗语言说明：

- 打开入口和再次启动方式；
- 已完成范围；
- 建议体验顺序；
- 已检查和未检查内容；
- 数据保存位置与当前限制；
- 可继续迭代的方向。

只有用户明确要求，或项目需要交给其他人验收时，才生成独立验收文档。

## 13. 非目标

- 不要求一次性穷尽所有未来需求；
- 不要求所有网站零依赖或双击 HTML 即运行；
- 不默认建设大型复杂平台的完整版本；
- 不要求普通用户理解开发流程或技术黑话；
- 不为每个阶段生成一份重复文档；
- 不把视觉稿选择、草案文件存在或构建成功误认为产品已经确认或交付完成。
