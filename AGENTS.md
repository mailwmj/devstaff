# AGENTS.md

## 用户画像端到端评测运行协议

本协议适用于用户要求进行“用户画像评测”“端到端回放”或“完整回归”。开始前必须阅读：

- [`tests/novice-user-evaluation.md`](tests/novice-user-evaluation.md)：画像、主线 UC、RC、评分、否决项和完整结论门槛。
- [`tests/site-design-scenarios.md`](tests/site-design-scenarios.md)：SD-01～SD-17 与 MV-01～MV-08 的输入和观察点。

本文只规定运行纪律；详细用例、固定业务事实和评分标准以上述文件为准。

### 1. 60 秒预检

在任何回放、写入或启动服务前，按以下顺序检查并记录：

1. `F-*` 夹具：确认本轮所需夹具存在、版本固定、可复制隔离，并检查初始状态/哈希。缺少任一必需夹具，完整评测立即标为“不完整”，不得临时伪造夹具或完整版本结论。
2. 两名真人：确认本版本有两名独立真人评测者，均按画像规则操作。两条模型跑、两条 Agent 会话或同一人两次，不得冒充两名真人。
3. 当前 Skill 版本：记录仓库四个 Skill 的版本/哈希，以及实际加载的安装路径和版本；发现不一致先标记风险，不得把未知版本结果写成版本回归结论。
4. 浏览器与端口：探测实际可用的浏览器/自动化能力、可用端口和已有进程；记录能力限制。不能浏览器实测的项目，视觉、响应式、交互和再次打开结论只能是 `not_run`。

预检记录至少包含：时间、工作区、副本、夹具版本、Skill 哈希、真人评测者、浏览器能力、PID/端口和限制。预检超时或任何检查未完成，不得跳过记录；直接按证据不足降级。

### 2. 评测分级与会话

- **完整评测**：具备全部必需 `F-*` 夹具、两名真人、浏览器/端口条件，并执行 UC-01～UC-04、RC-01～RC-25、SD-01～SD-17、MV-01～MV-08。所有非计划 `not_run` 都使结论保持“不完整”，不计算 A/B/C/D。
- **主线冒烟**：在夹具、真人或环境不足时，只回放 UC-01～UC-04 及明确可执行的关键路径，并明确标注“主线冒烟，不代表完整回归”。夹具不足时仍须标“不完整”，不得用临时输入补成完整版本。
- 从同一个干净 `F-EMPTY` 副本启动主线，并保持一个持续的被测 Agent/用户会话，保留对话、调用事件、状态和文件时间线。需要第二次画像回放时复制环境，但不要把模型双跑称作两名真人。
- 每个 RC/SD/MV 使用独立夹具副本；用例结束后销毁或重置，不继承上一用例的确认、授权、源码或 `stage="delivered"` 状态。

### 3. 门禁状态断言

每一轮开始和结束都读取并断言状态；不能只依据 Agent 的自然语言回执。至少维护：`stage`、`concept_confirmed`、`structure_required`、`structure_confirmed`、`visual_confirmed`、`development_authorized`、交付阶段（由 `stage="delivered"` 表示）、源码哈希、最近一次 check 结果，以及各道确认记录的 `basis`（`agent-reported` / `quote-matched`）。`structure_required=true` 而 `structure_confirmed=false` 表示用户只选了页面结构，视觉风格尚未确认。

- 方案未确认：不得正式设计或开发。
- 只有结构选择或只有视觉选择：只能记录对应的那一项（`structure_confirmed` 或 `visual_confirmed=true`），不得推断开发授权、创建正式源码或实施计划。用户只比较过页面结构时，风格方案这一步还没发生。
- 开发授权未明确：不得进入正式实施。
- `stage="delivered"`：必须绑定同一源码哈希的检查矩阵，所有阻断项为 `passed`，且没有未完成的再次打开验证。
- 用户改变核心范围、页面结构或高风险事项后，旧方案、结构确认、视觉确认、授权和交付状态必须失效并重新确认。

**门禁记录的是声明，不是同意的证明。** 断言时区分三件事，不得混为一谈：

1. **记录是否完整**：每条确认必须带用户原话（`--quote`），同一条原话不能连过两道门禁；这是脚本可机械核对的（`state.py show`）。
2. **标注是否诚实**：标注等级必须与实际证据相符。读不到或读不懂宿主会话记录时只能是 `agent-reported`；把声明写成或说成"已验证"即为否决项。脚本**不核验**原话是否真出自用户。
3. **同意是否成立**：只有人能判断。**由评测者读对话承担**——核对每条记录的原话在对话中确实存在、确实由用户说出、且出现在 Agent 记录它之前；简短答复还要核对它直接回应的上一问及已说明的行动后果，较早的持续实施委托要核对是否仍覆盖当前低风险范围；并由 `deliver` 回执的 `consent_replay` 在交付时念回给用户核对。

每次状态跃迁记录"前状态、用户原话、标注等级、后状态"。缺少原话或标注与证据不符的跃迁，立即按否决项处理并停止后续阶段。

### 4. Writer / Checker 隔离

Writer（builder 或修复 Agent）与独立 Checker 必须严格串行：

1. Writer 完成后完全退出，停止其体验稿/开发服务器及子进程；确认 PID 已结束、端口已释放或明确由正式服务占用。
2. 记录正式源码及关键产物的冻结哈希、运行根目录、启动命令和端口；冻结后不得再写入。
3. 仅在上述交接完成后启动独立 Checker。Checker 全程只读：不得编辑、格式化、安装会改写源码的依赖、重启 Writer 或修复发现的问题。
4. Checker 检查前后重新计算哈希；任一正式源码哈希变化，本轮独立验收立即作废，状态不得写为通过，必须回到 Writer 修复并重新完整检查。
5. Checker 的报告必须引用冻结哈希、实际 URL/视口、命令结果和截图/交互证据；静态阅读不能替代浏览器实测。

### 5. PID、端口与证据预算

- 每个服务登记 `owner / PID / 子 PID / port / root / start time / stop time`；禁止用“杀掉所有 Node”管理进程，禁止复用未确认归属的端口。
- 体验稿和正式站使用不同端口；正式验证前必须停止体验稿并确认 URL 指向正式根目录。结束用例时清理自有 PID 和端口，并记录无法清理的残留。
- 证据只保留能改变判定的最小集合：每个用例一条用户原话、一条状态快照、关键路径一组桌面/手机结果、失败复现与修复复验各一份、源码冻结哈希和必要日志。不得用大量截图替代结论或把截图当作点击证据。
- 每条 `passed / failed / not_run` 都必须能回指具体证据；无证据就是 `not_run`，不是推定通过。

### 6. 否决、停止与报告

触发任一一票否决项（越过门禁、伪造材料、未授权处理高风险数据/服务、未实测却声称通过、核心任务失败仍交付、Checker 修改源码、旧状态沿用等）后：

- 立即停止当前阶段和交付宣布，保存现场、状态、哈希、PID/端口及最小复现证据；不得继续操作把失败覆盖成通过。
- 能安全隔离时降级为“已执行部分不通过”；缺夹具、缺真人或非计划能力缺失则降级为“不完整/不定级”。计划内能力降级只能按方案规定的诚实 `not_run` 判定。
- 修复后必须从受影响门禁重新开始，并由独立 Checker 在新冻结哈希上复验；不得把修复前证据沿用到新版本。

报告顺序固定为：阻断项与否决项、核心路径结果、读写/状态证据、非阻断问题、环境限制与 `not_run`、再给完整 Skill 结论。明确区分“网站核心路径可用”与“整套 Skill 通过”：前者通过不等于后者通过；夹具或真人不全时只能报告局部观察，不能定级或宣布完整回归通过。
