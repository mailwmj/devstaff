# 渐进式建站：执行指令

这份文件写给 Agent。用户不需要看到其中的路径、状态或协议名。

## 唯一控制回路

```text
preflight → 执行 next_action → 产出结果 → 写入结果 → 再次 preflight
```

不要根据记忆猜下一步。`state.py preflight` 返回的 `next_action` 是当前唯一推荐动作；只执行 `allowed_actions`，不执行 `blocked_actions`。

`site-builder` 是唯一编排者。`site-brief`、`site-design`、`site-check` 完成任务后返回一次紧凑回执：`status / summary / artifacts / evidence / limitations`。它们不继续编排、不修改其他职责的产物、不宣布交付。

## 入口判断

| 请求 | 路径 |
| --- | --- |
| 明确 Bug、文案、颜色、间距局部修改 | `quick`，直接处理，不初始化 `.site` |
| 新建、整体改版或主流程变化 | `guided`，先收敛核心任务和方向 |
| 支付、权限、隐私、共享数据、公开部署或多人协作 | `strict`，要求独立验证和适用风险证据 |
| 只梳理需求 | 交给 `site-brief` 后停止 |
| 只要视觉方向或流程体验稿 | 交给 `site-design` 后停止 |
| 只要检查且没有修复请求 | 交给 `site-check` 后停止 |

有 `.site/state.json` 时先 `preflight`。已有项目的局部修改不因为没有状态而被迫初始化流程。

## 用户体验

- 每轮只让用户做一个高影响决定；新建、整体改版或主流程变化默认采用**“信息架构双选 ➔ 视觉风格双选（轻量且可继承）”**的敏捷递进确认。
- **信息架构双选**：轻量提供 2 种区分度大的信息架构方案，用通俗易懂的大白话说明适合谁、第一眼看到什么、怎么操作；同时直接轻量快速生成骨架网页（单文件纯灰阶布局，顶部带 A/B 切换），输出文件链接并优先引导在 Agent 客户端自带浏览器打开查看；用户选定后用 `select-structure` 记录。
- **视觉风格双选**：骨架确定后，在已定骨架上提供 2 种视觉气质差异显著的方案（如现代科技极简 vs 温暖杂志人文），直接生成轻量静态视觉预览页（单文件双风格切换）；此环节仅为直观感受调性，**严格禁止编写复杂业务代码，严格禁止执行单元测试、合同校验、全套浏览器深度断言等重度测试**，严控时间与 Token。
- **微调收敛与资产平滑继承**：支持用户对视觉 Demo 提出微调偏好（在单页 HTML/CSS 中快速修改响应）；用户满意的 Demo 是正式代码的胚胎，Demo 中的 CSS 变量提取为正式 Token，核心 HTML 结构直接作为第一条纵向切片的界面模板，用户微调偏好沉淀为 `VA-*` 验收标准，平滑过渡至正式开发，避免返工与预期漂移。双选闭环后用一句确认原话 `decide` 锁定完整方向。
- **不承诺绝对速度**：使用“轻量快速生成”等客观表述，不过度承诺“秒级”。
- 精简的是用户门禁，不是设计能力。新建和重大改版必须调用 `site-design`；由它按任务加载设计规范、模板、Token、素材规则和工艺审查。
- 用户明确要求建设且没有新的高风险歧义时，不重复索要“开发授权”。
- 信息不足时按当前前沿提问：用现实结果发问，每轮 3～5 题编号（Q1～Q5）附带推荐答案与一句话选项，手机上一屏读完，支持回复编号或“都按推荐方案”；低影响细节记为假设，不自动升级 strict。
- 事实、决定、假设、待确认严格分离：外部事实与推荐做法严禁代指用户决定；业务歧义当场指出核对。
- 不向用户展示状态名、模式或证据字段。

## 三条红线

1. 核心任务和方向未确认前，不写正式源码。
2. 高影响选择未确认前，不替用户决定。
3. 没有实际验证就不能说“已验证”；必须区分 `verified`、`limited`、`blocked`。
4. 体验稿与视觉预览阶段坚守轻量边界，不堆砌正式业务逻辑，严禁执行过度测试。

费用、系统变更、密钥、真实敏感数据和公开发布始终单独停下，等待用户明确决定。

## 实现循环

`building` 阶段按纵向切片推进，不先铺完整数据层、接口或所有页面再统一验证。每条切片在合同中引用 `BR-* / IC-* / PG-* / SC-* / CP-* / VA-*`，写明成功路径、最可能失败路径和必要状态。通用 Agent 执行切片时必须严格遵守以下三步循环，防遗忘、防乱写：

1. **读表锁定与状态锚定（防遗忘）**：每轮写代码前，必须先读取 `.site/design/surface-brief.md` 的纵向切片表，找到首个未完成切片并标记为进行中（`[-]`）。严禁脱离切片表凭注意力记忆跳步或跨切片开发。
2. **代码微计划与最小增量（防乱写）**：动手编码前，Agent 内部必须明确本次切片的三个技术要素（Micro-Spec）：
   - `Touch Files`：明确具体改动的目标文件与局部位置；
   - `Blast Radius`：检查是否会破坏前置切片已验证的 DOM 结构、CSS 类名或事件监听；
   - `Incremental Only`：坚持最小局部增量修改，严禁盲目全量重写已有代码文件。
3. **事实证据与闭环打勾（防假完成）**：切片内闭环合同引用检查、typecheck/lint、受影响单测和最短行为检查。检查通过后，必须在切片表中记录单行客观验证事实，方可打勾（`[x]`），再推进到下一条切片。

所有切片打勾完成后做一次集成构建，再由 `site-check` 只读浏览器验证；首次浏览器验证前合同、类型、构建和受影响测试必须已通过。失败只修当前发现，不重做整个项目。

## 交付

只说明入口、完成的核心任务、实际检查内容、未检查内容、数据位置和限制。不把“构建成功”说成“产品已验证”。
